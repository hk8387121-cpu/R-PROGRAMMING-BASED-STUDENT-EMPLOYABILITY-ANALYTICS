# 07_final_report.R
# This script aggregates all outputs and generates a markdown or PDF report
# using R Markdown (rmarkdown / knitr).

cat("Generating final analytical report...\n")
# rmarkdown::render("report_template.Rmd", output_format = "pdf_document", output_dir = "../results/")
cat("Report generated successfully.\n")
